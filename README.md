# aas-verify

# Implemented AAS Specifications

- AAS part1: Metamodel v3.0 Specification
- AAS test Engines

# Usage

    #step1. install library
        pip install fastapi
        pip install python-multipart

    #step2. start Server
        input cli : uvicorn main:app --reload
        open server
        - default address: http://127.0.0.1
        - port : 8000
